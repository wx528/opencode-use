[project]
name = "opencode-use"
version = "0.1.0"
description = "个人备忘 wiki,记录 opencode 用法的工作流与踩坑"
requires-python = ">=3.10"
dependencies = [
    "mkdocs>=1.5",
    "mkdocs-material>=9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []

[tool.uv]
package = false